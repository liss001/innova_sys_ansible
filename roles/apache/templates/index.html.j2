<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bienvenida a InnovaSys</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            flex-direction: column;
        }
        .container {
            background-color: #ffffff;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            text-align: center;
        }
        h1 {
            color: #007bff;
            margin-bottom: 20px;
        }
        p {
            font-size: 1.1em;
            line-height: 1.6;
        }
        .logo {
            width: 100px; /* Ajusta el tamaño si tienes un logo */
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>¡Bienvenidos a la Intranet de {{ nombre_empresa }}!</h1>
        <p>Estamos emocionados de tenerte aquí.</p>
        <p>Este es tu portal interno para acceder a recursos y herramientas.</p>
    </div>
</body>
</html>
